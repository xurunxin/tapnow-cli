# 首次验收：2026-09-10

环境：Windows / PowerShell 7 / Node.js 24.20.0 / Chrome；TapNow 页面 2.16.2。

## 已实际完成

- CLI 身份校验、项目列表、项目创建、3 节点和 2 连线同步、远端读回。
- 登录会话迁移到独立本地 Chrome 配置；后续 CLI 不依赖 Codex 工具或临时令牌文件。
- 默认浏览器模式刷新模型目录：82 条；模型转换与图像/视频报价读取。
- Banana 2 (`nano-banana-flash`)：16:9、1K、1 张图片；报价 5 Tapies，任务完成。
- Veo 3.1 Lite (`veo3.1-lite`)：上一步图片作为输入，4 秒、720p、无音频；报价 16 Tapies，任务完成。
- 结果写入原画布节点；图片下载 864,531 字节；视频下载 538,893 字节。
- 回填包含网页使用的 `options` 结果列表，并立即读回校验。关闭先前编辑页面后重新打开，图片实际加载、视频预览 duration 为 4 秒，连线仍为 2 条。
- 网页首次打开补充的 variant/text model 默认值不会被误判为用户修改；执行期间仍应避免其他页面同时编辑该画布。
- ffprobe：H.264，1280×720，4.000 秒，无音频流。
- 对图片再执行素材上传，返回有效文件 ID 与存储 URL。
- 重复 apply：0 新增、0 更新、0 新连线；重复 run：复用原任务，无新生成提交。
- 实际结果信封使用 `outputs` 对象；首次解析中断后修复，通过原任务 ID 恢复，未重复生图。

累计预估预留 21 Tapies。未单独读取完整账单流水，不能将预估数字当作最终账单核销证明。

## 本地自动化测试

覆盖重复/悬空/循环图、拓扑顺序、模型规格、业务错误信封、未知报价、费用阈值、缺少 execute、提交超时、未知提交阻断、完成任务续跑、输出解析、外部节点保留、外部配置冲突、无变化同步、运行不可变性、互斥锁与下载 URL 限制。

## 参数与 Skills 补充验收

2026-09-10 v0.2.0 参数与 Skills 补充验收：

- 37 项本地测试通过：新增 GPT 尺寸/质量、Seedance/H3 模式及输入限制、真实视频时长校验、参数修改失败保护、Agent 输出、安装幂等和冲突保护。
- 登录会话中，10 个 GPT Image/Seedance/H3 型号、31 个模式的网页转换成功，另验证 GPT 自动尺寸、4K 和自定义像素三种组合。
- 10 个型号的默认文生图/视频报价全部成功；报价而非实际生成，不以报价证明生成质量。
- 使用前次生成的 4 秒视频，浏览器实测时长为 4 秒；H3 reference_to_video 与 Seedance 2.5 video_edit 的输入转换和报价成功。
- 本轮没有新增收费生成。Skills 在临时目录安装、重复安装与修改冲突场景验证，不修改全局 Agent 技能目录。

本轮依据 TapNow 前端构建 `ace37eaf3306f9b329995803653e3d58ff3d632b`。验证了可转换与可报价，不代表全部媒体输入、计费结算或输出质量验收。

## 项目管理补充验收

2026-09-10 v0.3.0：52 项本地测试通过，覆盖画布变更冲突、未知提交保护、分组坐标、布局、评论版本游标、旧项目接管、角色版本和素材复用。

- 私有验收项目实测原生分组、嵌套分组、布局、交接文本、评论创建与回复，均远端读回并在网页检查。
- 私有素材库实测文件夹创建、已有图片入库、检索、角色创建和更新；旧 revision 更新被服务端拒绝。
- 角色参考素材和设定写入 H3 工作流并同步到画布，没有提交收费生成；旧项目导出为可继续维护的工作流。
- 钱包余额与独立额度查询成功；查询时可用余额为 55,112 Tapies，此数值仅代表查询时点。
- 团队转换已验证前端协议和本地计划、冲突保护及读回逻辑，未实际向团队共享验收项目。多人同时编辑的端到端验收仍未覆盖。

## 尚未覆盖

- 82 个目录条目的逐模型实际生成、复杂首尾帧/全模态/元素引用矩阵。
- 真实团队项目的多人协作与权限变更。
- 长时间认证过期、异常服务端降级及跨机器并发。
- 平台最终账单和额度消耗的独立对账。

账号 ID、令牌、画布内容、素材和原始站点 bundle 都不提交到仓库。验收画布与本地状态文件由操作用户保留。
